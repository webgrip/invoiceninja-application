namespace: invoiceninja-application

# Database passwords
mysql-password: ""  # REQUIRED: Strong password for MySQL invoiceninja user (matches invoiceninja.dbpass)
mysql-root-password: ""  # REQUIRED: Strong password for MySQL root user

# Invoice Ninja application secrets
app-key: ""  # REQUIRED: Generate with: php artisan key:generate --show (must start with "base64:")

# Mail configuration secrets (if using SMTP)
mail-password: ""  # REQUIRED: SMTP server password

# Additional secrets that may be needed
api-secret: ""  # OPTIONAL: API secret for external integrations
