services:
  invoiceninja-application.application:
    container_name: invoiceninja-application
    image: webgrip/invoiceninja-application:latest
    pull_policy: never
    build:
      context: .
      dockerfile: ops/docker/application/Dockerfile
    restart: always
    depends_on:
      invoiceninja-application.mariadb:
        condition: service_healthy
      invoiceninja-application.redis:
        condition: service_healthy
    ports:
      - "127.0.0.1:9000:9000"
    env_file: ./.env
    environment:
      DB_TYPE: ${DB_CONNECTION:-}
      DB_STRICT: "false"
      LOG_CHANNEL: stack
      REQUIRE_HTTPS: "false"
    volumes:
      - ./.env:/var/www/app/.env
      - invoiceninja-application-application-public-data:/var/www/app/public
      - invoiceninja-application-application-storage-data:/var/www/app/storage

  invoiceninja-application.nginx:
    container_name: invoiceninja-application-nginx
    image: webgrip/invoiceninja-application-nginx:latest
    pull_policy: never
    build:
      context: .
      dockerfile: ops/docker/nginx/Dockerfile
    restart: always
    depends_on:
      - invoiceninja-application.application
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - invoiceninja-application-application-public-data:/var/www/app/public:ro

  invoiceninja-application.mariadb:
    container_name: invoiceninja-application-mariadb
    image: webgrip/invoiceninja-application-mariadb:latest
    build:
      context: .
      dockerfile: ops/docker/mariadb/Dockerfile
    pull_policy: never
    restart: unless-stopped
    ports:
      - "127.0.0.1:3306:3306"
    logging:
      options:
        max-size: 10m
    env_file:
      - ./.env
    environment:
      MARIADB_DATABASE: ${DB_DATABASE:-ninja}
      MARIADB_USER: ${DB_USERNAME:-ninja}
      MARIADB_PASSWORD: ${DB_PASSWORD:-ninja}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-root}
      MARIADB_CHARACTER_SET_SERVER: utf8mb4
      MARIADB_COLLATION_SERVER: utf8mb4_unicode_ci
    volumes:
      - invoiceninja-application-mariadb-data:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD",
          "mariadb",
          "--user=${DB_USERNAME:-ninja}",
          "--password=${DB_PASSWORD:-ninja}",
          "--execute=SELECT 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  invoiceninja-application.redis:
    container_name: invoiceninja-application-redis
    image: webgrip/invoiceninja-application-redis:latest
    pull_policy: never
    build:
      context: .
      dockerfile: ops/docker/redis/Dockerfile
    restart: always
    volumes:
      - invoiceninja-application-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 5s

volumes:
  invoiceninja-application-application-storage-data:
  invoiceninja-application-application-public-data:

  invoiceninja-application-mariadb-data:
  invoiceninja-application-redis-data:
  certificate-data:
    external: true

networks:
  default:
    external: true
    name: webgrip
